# PIM Implementation and Configurations

*(Source pages 826–886 of the original export. Page 826, a "Thank you" slide belonging to the preceding course module, is not part of this topic and has been excluded from this file. This document begins at the actual title slide, page 827.)*

## Foreword

- A multicast network consists of multicast sources, multicast group members, and multicast routers.
  - A multicast source is mainly used to send multicast data.
  - Multicast group members receive multicast data. Therefore, IGMP must be used to notify the multicast network of the locations of group members and the groups that the members join.
  - Multicast routers forward data from a multicast source to multicast group members. Multicast data forwarding depends on multicast distribution trees (MDTs). Therefore, multicast routers need to use protocols to establish MDTs.
- The Protocol Independent Multicast (PIM) protocol is mainly used to establish MDTs.
- This course describes basic concepts of PIM and fundamentals of PIM-DM and PIM-SM.

## Objectives

- On completion of this course, you will be able to:
  - Describe the basic concepts of PIM.
  - Describe the fundamentals of PIM-DM.
  - Perform basic configurations of PIM-DM.
  - Describe the fundamentals of PIM-SM.
  - Perform basic configurations of PIM-SM.

## Contents

1. Introduction to PIM
2. Introduction to PIM-DM
3. Introduction to PIM-SM

## 1. Introduction to PIM

### Multicast Network Architecture Review

- A multicast network can be divided into three parts:
  - Source end network: sends multicast data generated by the multicast source to the multicast forwarding network.
  - Multicast forwarding network: generates a loop-free multicast forwarding path, which is also referred to as an MDT.
  - Receiver end network: uses IGMP to enable the multicast network to detect the locations of multicast group members and the multicast groups that the members join.

Diagram description: A left-to-right topology divided into three labeled zones. The Source end network (left) contains a multicast source device connected to a multicast router acting as the First Hop Router (FHR). The Multicast forwarding network (middle) shows a cloud of interconnected multicast routers running the multicast routing protocol, forming the MDT. The Receiver end network (right) shows a multicast router acting as the Last Hop Router (LHR) connected to two multicast group member devices, one joining multicast group 1 and the other joining multicast group 2. Dashed vertical lines separate the three zones, with annotations: "Sends multicast data to the multicast forwarding network" under the source end, "An MDT is established based on the multicast routing protocol" under the forwarding network, and "IGMP is used to obtain multicast group members' locations and the multicast groups that they join" under the receiver end.

- The receiver end network uses IGMP to enable the multicast network to detect the locations of multicast group members and the multicast groups that the members join.
- MDT establishment on the multicast forwarding network requires a multicast routing protocol.
- There are multiple multicast routing protocols. The most commonly used one is PIM, which is the focus of this course.

### Multicast Data Forwarding Process Review

- The multicast data forwarding process is as follows:

Diagram description: A multicast source (192.168.10.1) sends a payload addressed to multicast group 239.0.0.1 into a network of four routers (RT1–RT4) forming the "Multicast network," toward a multicast group member that has joined 239.0.0.1. Each router is shown with its multicast routing table alongside it, listing Multicast Information (S: 192.168.10.1, D: 239.0.0.1), Inbound Interface, and Outbound Interface columns (e.g., IF1/IF2/IF3 depending on the router), plus an RPF route table showing Destination Network 192.168.10.1 mapped to RPF Interface IF1. A callout box on RT1 asks "How is an MDT generated?", highlighting that the diagram illustrates how the multicast routing table and RPF check combine to build the forwarding tree.

- The course "IGMP Implementation and Configurations" describes how a multicast network discovers the locations of multicast group members and the multicast groups that the members join.
- This course mainly describes how an MDT is established.

### Multicast Protocol Review

- A multicast network needs to establish forwarding paths based on multiple multicast protocols.
  - IGMP runs on the receiver end network and is used to inform the multicast network of the locations of group members and the multicast groups that the members join.
  - Protocols working on the multicast forwarding network include PIM, MSDP, and MBGP.
    - PIM is mainly used to generate MDTs in an AS.
    - MSDP is mainly used to help generate inter-AS MDTs.
    - MBGP is used to help perform RPF check on inter-AS multicast traffic.

Diagram description: Two autonomous systems, "Multicast network AS 100" (left) and "Multicast network AS 200" (right), each drawn as a cloud of PIM-speaking routers. A multicast source connects into AS 100, and a multicast group member (via IGMP) connects out of AS 200. The two ASes are interconnected by border routers exchanging MSDP and MBGP, shown as bidirectional arrows labeled "MSDP" and "MBGP" between the two clouds.

- This course mainly describes the implementation of PIM.

### PIM Introduction

- PIM is a multicast routing solution that performs a reverse path forwarding (RPF) check on multicast packets based on the unicast routing table, creates multicast routing entries if multicast packets pass the RPF check, and forwards the multicast packets using multicast routing entries. PIM is termed protocol-independent because it is not dependent on any particular unicast routing protocol for topology discovery.
- Currently, there are two PIM modes on the live network:
  - PIM-Dense Mode (PIM-DM)
  - PIM-Sparse Mode (PIM-SM): According to the multicast service model, PIM-SM can be classified into the following modes:
    - PIM-SM (ASM): sets up an MDT for any-source multicast (ASM).
    - PIM-SM (SSM): sets up an MDT for source-specific multicast (SSM).
- The commonly used PIM version is PIMv2. PIMv2 messages are encapsulated in IP packets, carrying the protocol ID 103 and group address 224.0.0.13.
- In a PIM domain, a P2MP multicast forwarding path is set up from a multicast source to group members for each multicast group. A multicast forwarding path looks like a tree, so it is also called an MDT.
- Characteristics of an MDT:
  - Each link transmits at most one copy of identical data, regardless of how many group members exist on the network. The multicast data is replicated and distributed on a bifurcating node as far from the source as possible.

### Usage Scenarios of PIM-DM and PIM-SM

- PIM MDTs can be established in PIM-DM or PIM-SM mode. The two modes are used in different scenarios:
  - The PIM-DM mode is mainly used on a multicast network with a small number of densely distributed group members. The basic idea of establishing an MDT in this mode is "flooding-prune". That is, multicast traffic is flooded on the entire network, and then the paths without group members are pruned to form an MDT.
  - The PIM-SM mode is mainly used on a multicast network with a large number of sparsely distributed group members. To establish an MDT in this mode, information about group members needs to be collected first. The PIM-SM mode does not flood multicast packets on the entire network and has little impact on the live network. Therefore, the PIM-SM mode is usually used on the live network.

### Classification of MDTs

- MDTs established using PIM are classified into the following types:
  - An MDT with the multicast source as the root and group members as leaves is referred to as a shortest path tree (SPT). SPTs are used in both PIM-DM and PIM-SM.
  - An MDT with a rendezvous point (RP) as the root and group members as leaves is referred to as an RPT. RPTs are used in PIM-SM.

Diagram description: Two simple tree diagrams side by side. The left tree is rooted at the multicast source with branches fanning out directly to multicast group members — this is the SPT. The right tree is rooted at an RP (rendezvous point), with branches fanning out to multicast group members — this is the RPT.

- SPTs are also called source trees and are used on both PIM-DM and PIM-SM networks.
- RPTs are mainly used on PIM-SM networks.
- For details about RP functions, see the chapter "PIM-SM".

### PIM Routing Entries

- PIM routing entries are created using PIM to guide multicast forwarding.
- There are two types of routing entries on a PIM network:
  - (S, G) routing entries are mainly used to establish SPTs on a PIM-DM or PIM-SM network.
  - (*, G) routing entries are mainly used to establish RPTs on a PIM-SM network.

Diagram description: Two example PIM routing-table entries are shown side by side, a (*, G) entry and an (S, G) entry, reproduced verbatim in the code blocks below.

**PIM (\*, G) routing entry:**
```
(*, 239.0.0.1)
RP: (unspecified/blank in source)
Protocol: pim-sm, UpTime: 02:07:35, Expires: -
Upstream interface: NULL
Upstream neighbor: NULL
RPF prime neighbor: NULL
Downstream interface(s) information:
Total number of downstreams: 1
1: GigabitEthernet0/0/0
Protocol: pim-sm, UpTime: 02:07:35, Expires: -
```

**PIM (S, G) routing entry:**
```
(1.1.1.1, 239.0.0.1)
Protocol: pim-dm, Flag: ACT
UpTime: 02:10:27
Upstream interface: GigabitEthernet0/0/2
Upstream neighbor: 10.0.12.2
RPF prime neighbor: 10.0.12.2
Downstream interface(s) information:
Total number of downstreams: 1
1: GigabitEthernet0/0/0
Protocol: pim-sm, UpTime: 02:10:27, Expires: -
```
*(Note: the source PDF's rendering of the (\*, G) entry's RP field and protocol/uptime line was partially obscured by an overlapping diagram element; the reconstruction above preserves all legible fields. Recommend cross-checking this specific entry against the original slide if exact wording of the RP line is needed.)*

- S indicates a specific multicast source, G indicates a specific multicast group, and * indicates any multicast source.
- A PIM router may have both (S, G) and (*, G) entries. When the router receives a multicast packet with the source address S and the group address G, the router forwards the packet according to the following rules if the packet passes the RPF check:
  - If a matching (S, G) entry exists, the router forwards the packet according to the (S, G) entry.
  - If no matching (S, G) entry exists but a matching (*, G) entry exists, the router creates an (S, G) entry based on the (*, G) entry, and forwards the packet according to the (S, G) entry.
- For details about the flag description, see the next slide.

### PIM Routing Entries (Flag Field Reference)

- The Flag field in the PIM routing table is described as follows:

| Flag Value | Description |
|---|---|
| 2msdp | The RP recently received a Registration message, learned (S, G) information from the message, and is about to notify MSDP that the next SA message will contain the (S, G) entry. |
| act | Multicast data corresponding to the multicast routing entry has arrived. |
| del | Multicast routing entry to be deleted. |
| exprune | The entry on the RPT is pruned, and no receiver on the RPT requests the information sent by the source. |
| ext | The routing entry contains an outbound interface provided by another multicast routing protocol. |
| loc | The routing entry is on the device directly connected to the network segment of the multicast source. |
| msdp | The routing entry was learned by the RP from a received MSDP SA message. |
| niif | The routing entry contains an unknown inbound interface. |
| nonbr | The routing entry has no upstream neighbor address (link-local address) to the RP or source. |
| none | The routing entry has no flag. |
| rpt | The routing entry is on the RPT but does not use RPT data. |
| sg_rcvr | The router has receivers of the source specified in the (S, G) entry, and PIM is the owner of the downstream interface. |
| sgjoin | The router has receivers of the source specified in the (S, G) entry, but PIM is not the owner of the downstream interface. |
| spt | The routing entry is on the SPT. |
| swt | The routing entry is generated during the RPT-to-SPT switchover. |
| upchg | Route change flag, indicating that the current entry uses the original upstream device to forward data and is waiting for data from a new interface. |
| wc | (*, G) entry. |

*(Note: the flag value transcribed as "2msdp" in the source table is transcribed exactly as it appears; this may be a source rendering of a flag that other Huawei documentation lists simply as an RP/registration-related flag — worth double-checking against the original slide image if precision on this specific row matters.)*

### PIM Routing Entries and Multicast Routing Entries

- On different multicast routers, multicast routing entries are summarized based on different entries.
  - Multicast routing entries of the last-hop router are mainly generated based on PIM routing entries, IGMP group entries, and IGMP routing entries.
  - Other multicast routers generate multicast routing entries based on PIM routing entries.

Diagram description: An example multicast routing entry is shown for a router, illustrated below.

```
(1.1.1.1, 239.0.0.1)
Protocol: pim-dm, Flag: ACT
Uptime: 00:00:27
Upstream interface: GigabitEthernet1/0/1
RPF prime neighbor: 10.0.12.2
Downstream interface(s) information:
Total number of downstreams: 1
1: GigabitEthernet0/0/0
Protocol: pim-dm, UpTime: 00:00:27, Expires: -
```

- For details about multicast routing entry generation on the last-hop router, see "IGMP Implementation and Configurations".
- Multicast routing entries can be generated only based on PIM (S, G) routing entries. PIM (*, G) entries do not contain inbound interface information, and as a result, no multicast routing entries can be generated accordingly.

## 2. Introduction to PIM-DM

### Contents (section marker)

1. Introduction to PIM
2. Introduction to PIM-DM
   - Implementation of PIM-DM
   - Basic PIM-DM Configuration
3. Introduction to PIM-SM

### Basic Concepts of PIM-DM

- PIM-DM is mainly used on a network with a small number of densely distributed group members. PIM-DM uses the flooding-prune mechanism to establish an MDT.
- In addition to the flooding and prune mechanisms, PIM-DM also involves the neighbor discovery, graft, assert, and state-refresh mechanisms.

Diagram description: A simple network of multicast routers is shown with a multicast source on the left and a multicast group member on the right, with an arrow labeled "MDT generation" indicating the overall process the following slides will break down into its component mechanisms (flooding, prune, neighbor discovery, graft, assert, state-refresh).

### PIM-DM Messages

- PIM messages are encapsulated in IP packets, with destination address 224.0.0.13 and IP protocol number 103.
- PIM-DM and PIM-SM use different types of messages.
- PIM-DM uses the following types of messages:

| Message Type | Message Function |
|---|---|
| Hello | Used for PIM neighbor discovery, protocol parameter negotiation, and PIM neighbor relationship maintenance. |
| Join/Prune | Join messages are used to add nodes to the MDT, whereas Prune messages are used to remove nodes from the MDT. Join and Prune messages are in the same format in PIM, but they have different field contents in the payload. |
| Graft | Used to graft a node to the MDT. |
| Graft-ACK | Used to acknowledge the Graft messages sent by neighbors. |
| Assert | Used in the Assert mechanism. |

### Neighbor Discovery

- An MDT can be established only between PIM neighbors. Therefore, neighbor discovery is the prerequisite for MDT establishment. Neighbor discovery is implemented through PIM Hello messages.

Diagram description: Two routers exchange PIM Hello messages (destination address 224.0.0.13) back and forth. A sequence/timeline shows: a Hello message is sent, a Hello message is received, and the neighbor relationship is created; the process repeats periodically, and if a Hello message is not received from a neighbor within the hold timer, the neighbor relationship is deleted.

- Each PIM interface periodically sends PIM Hello messages with destination address 224.0.0.13. After exchanging Hello messages, multicast routers can learn neighbor information and establish PIM neighbor relationships.
- A Hello message carries the following PIM protocol parameters to control PIM message exchanges between PIM neighbors:
  - DR_Priority: indicates the priority used for DR election among interfaces. The interface with the highest priority becomes the DR.
  - Holdtime: timeout period during which the neighbor remains in the reachable state. If a router does not receive any Hello message from its PIM neighbor within the timeout period, the router considers the neighbor unreachable.
  - LAN_Delay: indicates the delay in transmitting Prune messages on a shared network segment.
  - Neighbor-Tracking: indicates the neighbor tracking function.
  - Override-Interval: indicates the interval for overriding the prune action.

### MDT Establishment for the First Time (PIM-DM)

*(Slide header shows the running section labels: MDT Establishment | MDT Maintenance | MDT Update, with "MDT Establishment" highlighted as current.)*

- In PIM-DM mode, an MDT is established for the first time based on the flooding, prune, assert, and DR election mechanisms.
- Assert mechanism: When a multicast network exists during multicast traffic forwarding, a multicast forwarding router needs to be elected.

Diagram description: A multicast source sends traffic that floods across the network to all routers, reaching a multicast group member. Path pruning is then performed on branches with no group members, using the assert mechanism (which determines the multicast forwarding router) and Prune messages, converging on a final loop-free MDT to the group member.

### Flooding Mechanism

*(Running header: MDT Establishment | MDT Maintenance | MDT Update)*

- The multicast packets sent by the multicast source are flooded on the entire network. When a PIM router receives a multicast packet, it performs the RPF check on the packet. If the packet passes the RPF check, the router creates an (S, G) entry and sends the packet to all PIM neighbors.
- The (S, G) entry generated by PIM-DM has an aging time (210s by default). If no new multicast packet is received throughout the aging time, the (S, G) entry is deleted.
- The flooding process is as follows: the multicast source sends data; does the multicast group member exist on the network? Do routers without group members need to receive multicast data? The data is flooded to all routers regardless, and an (S, G) entry is created/updated at each router as multicast traffic is received.

Diagram description: The multicast source at the left sends a packet that is flooded hop-by-hop across all routers in the network (illustrated with dashed arrows fanning out to every router), reaching a multicast group member at the right. Each router shown creates a local (S, G) multicast routing entry as the flood passes through it.

- According to the flooding mechanism, multicast data is flooded on the entire network periodically (180s by default). The main purpose of periodic flooding is to detect whether new members join a group. However, the flooding of multicast data on the entire network wastes a large amount of bandwidth. Therefore, the state-refresh mechanism plus graft mechanism is generally used to listen for new members periodically on the entire network.

### Assert Mechanism

*(Running header: MDT Establishment | MDT Maintenance | MDT Update)*

- When multiple PIM routers are connected on a network segment, the assert mechanism is used to select only one of them to forward multicast packets on the network segment.
- The election rules of the assert mechanism determine the forwarding behavior of each multicast router.

Diagram description: Two or more PIM routers sit on the same shared network segment, both having received the same flooded multicast packet. They exchange Assert messages across the segment to elect a single forwarder; the diagram marks the active PIM interface and active IGMP interface after the election, with the losing router's interface shown as inactive for forwarding.

- The assert mechanism is triggered by multicast data.

### Election Rules in the Assert Mechanism

*(Running header: MDT Establishment | MDT Maintenance | MDT Update)*

- After a PIM router receives the same multicast packet from its neighbor, it sends an Assert message to the network segment for assert election. The Assert message carries the unicast route preference and cost to the multicast source. Election rules are as follows:
  1. If the routers have different unicast route preferences, the router with the highest unicast route preference wins.
  2. If the routers have the same unicast route preference, the router whose route to the multicast source has the smallest cost wins.
  3. If the routers have the same unicast route preference and the same route cost to the multicast source, the router whose downstream interface has the highest IP address wins.

Diagram description: The diagram repeats the shared-segment scenario from the previous slide, now labeling the winning router as "the assert winner" (marked with an active PIM interface icon) and the losing router's interface as suppressed, with the multicast group member shown joining the group beyond the winning router.

- Assert losers suppress multicast data forwarding and retain the Assert state for a period of time (180s by default).
- After the assert timer expires, the assert losers trigger a new round of election.

### Prune Mechanism

*(Running header: MDT Establishment | MDT Maintenance | MDT Update)*

- For a multicast router that is not connected to any group member, the multicast network does not need to send multicast traffic to the multicast router. Through the prune mechanism, the multicast network can prune this type of path.
- The prune mechanism is as follows: after receiving multicast traffic on a segment with no group members, the router's multicast routing table shows no outbound interface for the group; the router therefore triggers the prune mechanism and sends a Prune message upstream, updating the outbound interface list and removing itself from the MDT.

Diagram description: A branch of the flooded tree leads to a router with no attached group members. That router sends a Prune message back up the tree toward the source. The upstream router removes the corresponding downstream interface from its (S, G) entry's outbound interface list, and multicast traffic stops flowing down that pruned branch, leaving a loop-free MDT to only the branches with active group members.

- The pruned downstream interface on a PIM router starts a prune timer (210s by default) and resumes multicast forwarding after the timer expires. Subsequently, multicast packets are flooded throughout the entire network and new group members can receive multicast packets from the interface. If a leaf router connected to a network segment that has no group members receives the flooded multicast packets, the leaf router initiates the prune mechanism. PIM-DM updates the SPT periodically through the process of periodic flooding and prune.
- After a downstream interface of a leaf router is pruned, the leaf router will initiate either the graft or state-refresh mechanism:
  - When new members join a multicast group on the network segment connected to the leaf router and want to receive multicast packets before the prune timer expires, the leaf router initiates the graft mechanism.
  - When no member joins a multicast group on the network segment connected to the leaf router and the downstream interface still needs to be suppressed, the leaf router initiates the state-refresh mechanism.

### MDT Maintenance (PIM-DM)

*(Running header: MDT Establishment | MDT Maintenance | MDT Update)*

- An MDT may be updated or even removed.
- When PIM neighbor relationships are stable and group memberships remain unchanged, MDTs can be maintained in either of the following ways:
  - Multicast packets are continuously sent to ensure that multicast routing entries always exist.
  - State-Refresh messages are sent to ensure that the downstream interface status in multicast routing entries remains unchanged.

Diagram description: The same pruned-tree topology as previous slides is shown, with a non-group-member branch remaining pruned/suppressed while State-Refresh and Assert messages continue to circulate to keep the rest of the MDT's state current.

### State-Refresh Mechanism

*(Running header: MDT Establishment | MDT Maintenance | MDT Update)*

- To prevent a pruned interface from resuming multicast packet forwarding on a PIM-DM network after the prune timer expires, the first-hop router nearest to the multicast source periodically sends State-Refresh messages throughout the entire PIM-DM network.
- PIM routers receiving the State-Refresh messages reset the prune timer. If the pruned interface on a downstream leaf router keeps having no group members, the interface remains suppressed.
- State-Refresh messages are sent at an interval of 60s by default.

Diagram description: The first-hop router nearest the multicast source originates a State-Refresh message that propagates hop-by-hop through every router in the MDT (shown with dashed arrows spreading outward), resetting each pruned interface's prune timer along the way so that already-pruned branches (with no group members) remain pruned instead of reverting to flooding.

### New Members Joining a Group

*(Running header: MDT Establishment | MDT Maintenance)*

- After new members join a multicast group, the multicast network needs to update the MDT so that the new members can receive multicast data. In PIM-DM mode, after an MDT is established in flooding-prune mode, the downstream interface cannot be restored automatically once being suppressed through the state-refresh mechanism.
- Therefore, some mechanisms are required to update MDTs. In PIM-DM mode, MDTs can be updated in either of the following methods:
  - Wait for multicast routing entries to time out, and then packets are flooded on the entire network again. This method is uncontrollable and cannot be implemented on the live network.
  - Use the graft mechanism. With the graft mechanism, when a new member joins a group, the leaf PE that receives the Report message of this member proactively sends a Graft message upstream to establish a reverse MDT. On the live network, the graft mechanism is often used in this case.

### Graft Mechanism

*(Running header: MDT Establishment | MDT Maintenance)*

- PIM-DM uses the graft mechanism to enable new group members on a pruned network segment to rapidly obtain multicast data.
- IGMP helps a leaf router learn whether new group members have joined a multicast group on the connected network segment. If a new group member joins the multicast group, the leaf router sends a Graft message to the upstream router according to the local multicast routing table, requesting the upstream router to restore the forwarding capability of the corresponding outbound interface and add the outbound interface to the downstream interface list of the (S, G) entry.

Diagram description: On a previously pruned branch, a new group member sends an IGMP Report message to its directly connected leaf router. The leaf router, seeing the new member, sends a Graft message upstream toward the source. The upstream router restores the corresponding outbound (downstream) interface and adds it back to the (S, G) entry, resuming multicast forwarding down that branch to the new member.

## Implementation of PIM-DM — Basic PIM-DM Configuration

### Contents (section marker)

1. Introduction to PIM
2. Introduction to PIM-DM
   - Implementation of PIM-DM
   - **Basic PIM-DM Configuration**
3. Introduction to PIM-SM

### Basic PIM-DM Configurations

1. Enable multicast routing on a router.
```
[Huawei] multicast routing-enable
```
2. Enable PIM-DM on an interface.
```
[Huawei-GigabitEthernet1/0/0] pim dm
```
3. Check PIM neighbor parameters.
```
<Huawei> display pim neighbor
```
4. Check the PIM routing table.
```
<Huawei> display pim routing-table
```

### PIM-DM Basics Experiment

- Experiment requirements:
  - Configure PIM-DM to allow PC1 and PC2 to receive data packets from the multicast source.

Diagram description: A topology with a multicast source (10.10.10.10/24) connected via interface GE0/0/2 to router R1. R1 connects to R2 via GE0/0/0 on each side, and to R3 via GE0/0/1 (R1) to GE0/0/0 (R3). R2's GE0/0/1 connects to PC1 (Receiver, joined to multicast group 224.1.1.1). R3's GE0/0/1 connects to PC2 (Receiver, also joined to multicast group 224.1.1.1).

### PIM-DM Configurations (1)

Configurations on R1:
```
[R1] multicast routing-enable
[R1] interface g0/0/2
[R1-GigabitEthernet0/0/2] pim dm
[R1-GigabitEthernet0/0/2] interface g0/0/0
[R1-GigabitEthernet0/0/0] pim dm
[R1-GigabitEthernet0/0/0] interface g0/0/1
[R1-GigabitEthernet0/0/1] pim dm
```

Configurations on R2:
```
[R2] multicast routing-enable
[R2] interface g0/0/0
[R2-GigabitEthernet0/0/0] pim dm
[R2-GigabitEthernet0/0/0] interface g0/0/1
[R2-GigabitEthernet0/0/1] igmp enable
```

*(Configurations of interface IP addresses and OSPF are not provided here — as stated in the source slide.)*

### PIM-DM Configurations (2)

Configurations on R3:
```
[R3] multicast routing-enable
[R3] interface g0/0/0
[R3-GigabitEthernet0/0/0] pim dm
[R3-GigabitEthernet0/0/0] interface g0/0/1
[R3-GigabitEthernet0/0/1] igmp enable
```

*(Configurations of interface IP addresses and OSPF are not provided here — as stated in the source slide.)*

### Checking the PIM Routing Table

```
<R1> display pim routing-table
 VPN-Instance: public net
 Total 0 (*, G) entry; 1 (S, G) entry
 (10.10.10.10, 224.1.1.1)
     Protocol: pim-dm, Flag: LOC ACT
     UpTime: 00:00:34
     Upstream interface: GigabitEthernet0/0/2
     Upstream neighbor: NULL
     RPF prime neighbor: NULL
     Downstream interface(s) information:
     Total number of downstreams: 2
         1: GigabitEthernet0/0/0
             Protocol: pim-dm, UpTime: 00:00:34, Expires: never
         2: GigabitEthernet0/0/1
             Protocol: pim-dm, UpTime: 00:00:34, Expires: never
```

## 3. Introduction to PIM-SM

### Contents (section marker)

1. Introduction to PIM
2. Introduction to PIM-DM
3. Introduction to PIM-SM
   - Implementation of PIM-SM (ASM)
   - Implementation of PIM-SM (SSM)
   - Basic PIM-SM Configurations

### PIM-DM Limitations

- If PIM-DM is used on a large- or medium-sized multicast network, the following problems may occur:
  - The flooding-prune mode is used, and multicast packets are flooded on the entire network, which burdens the network.
  - All multicast routers need to maintain a multicast routing table even if they do not need to forward multicast data.
  - On a multicast network with sparsely distributed group members, the efficiency of MDT establishment using the flooding-prune mechanism is low.

Diagram description: A large- or medium-sized multicast network is shown flooded end-to-end with multicast traffic (dashed arrows reaching every router), burdening the network, even though only one router at the edge has an attached group member — illustrating the inefficiency PIM-DM has at this scale.

### Introduction to PIM-SM (ASM)

- The PIM-DM model uses flooding-prune to establish an MDT because most multicast routers on the multicast network do not know the locations of group members.
- In the PIM-SM (ASM) model, an MDT is established as follows:
  - A rendezvous point (RP) is informed of the locations of group members in advance so that an RPT is established.
  - After receiving multicast data from the multicast source, the multicast network sends the multicast data to the RP, which then forwards the multicast data to group members.
  - If sub-optimal multicast forwarding paths exist, PIM-SM (ASM) automatically uses the SPT.

Diagram description: A multicast source sends data into the network toward an RP. Routers not on the forwarding path do not need to maintain multicast routing state. The RP forwards the multicast data down the RPT to the group member, illustrating that only the source-to-RP-to-receiver path carries multicast traffic rather than the whole network being flooded.

- MDT establishment using the PIM-SM (ASM) model has the following advantages:
  - Only the multicast routers on the multicast forwarding path need to maintain the multicast routing table.
  - The RP enables all multicast routers to know the locations of group members.
  - The flooding-prune mechanism is avoided, which improves MDT establishment efficiency.

### PIM-SM (ASM) Messages

- PIM messages are encapsulated in IP packets, with destination address 224.0.0.13 and IP protocol number 103.
- PIM-SM uses the following types of messages:

| Message Type | Message Function |
|---|---|
| Hello | Used for PIM neighbor discovery, protocol parameter negotiation, and PIM neighbor relationship maintenance. |
| Register | Unicast message used for source registration. During the source registration process, the first-hop router (source DR) encapsulates multicast data into a unicast Register message and sends the message to the RP. |
| Register-Stop | Used by the RP to instruct the source DR to stop sending Register messages, after multicast data starts being forwarded natively along the SPT/RPT. |
| Join/Prune | Join messages are used to add nodes to the MDT, whereas Prune messages are used to remove nodes from the MDT. |
| Assert | Used in the assert mechanism. |
| Bootstrap | Used for BSR election. In addition, the BSR also uses Bootstrap messages to flood the summary information about candidate-RPs (C-RPs) on the network. |
| Candidate-RP-Advertisement | Sent by a C-RP to the BSR, carrying information about the C-RP, such as the IP address and priority. |

*(Note: the source slide's row for "Register-Stop" was not fully legible in this transcription pass; its function is documented explicitly on the later "Multicast Source Registration Mechanism — Multicast Data Forwarding" slide, and the description above is drawn from that slide's content for consistency. Recommend a visual check against the original if the exact wording of this table row is needed.)*

### RP Introduction

- An RP is an important PIM router on the network. It processes the Register messages from the source DR as well as the Report messages from group members. All PIM routers on the network must know the address of the RP. An RP is similar to a convergence center of demand and supply information.
- An RP can be configured manually or elected dynamically.
  - Static RP: The same RP address is configured for all PIM routers on the network.
  - Dynamic RP: An RP is elected among multiple C-RPs through an election mechanism.

Diagram description: A multicast source and a multicast group member sit on opposite sides of a large- or medium-sized network, with a single router in the middle marked as the RP, illustrating the RP's role as the convergence point between source-side registration and receiver-side joins.

- You can specify the multicast groups for which a static or dynamic RP provides services.

### Dynamic RP Election

- Dynamic RP election involves two roles: candidate-bootstrap router (C-BSR) and C-RP.
  - A unique BSR is elected from C-BSRs.
  - The BSR collects C-RP information to form an RP-Set and floods the RP-Set to all other PIM routers through PIM messages.
  - After receiving the message with the RP-Set, these PIM routers elect an RP according to RP election rules.

Diagram description: Multiple candidate-BSR routers exchange Bootstrap messages to elect a single BSR. Multiple candidate-RP routers send Candidate-RP-Advertisement messages to the elected BSR. The BSR then floods Bootstrap messages (carrying the RP-Set) to all other PIM routers on the network, which use this information to independently compute the same RP for each multicast group.

- The BSR election rules are as follows:
  - If the C-BSRs have different priorities, the C-BSR with the highest priority (largest priority value) is elected as the BSR.
  - If the C-BSRs have the same priority, the C-BSR with the highest IP address is elected as the BSR.
- The RP election rules are as follows:
  - The C-RP with the longest mask length of the served group address range matching the specific multicast group wins.
  - If an RP cannot be elected based on the preceding rule, the C-RP with the highest priority (smallest priority value) wins.
  - If an RP cannot be elected based on the preceding rules, the hash function is executed. The C-RP with the largest calculation result wins.
  - If an RP cannot be elected based on the preceding rules, the C-RP with the highest IP address wins.

### MDT Establishment for the First Time (PIM-SM ASM)

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- In the PIM-SM (ASM) model, MDT establishment for the first time depends on the RPT establishment mechanism, multicast source registration mechanism, and DR election mechanism.
  - RPT establishment mechanism: A multicast leaf router proactively establishes an MDT (RPT) to the RP.
  - Multicast source registration mechanism: used to establish an MDT (SPT) from the multicast source to the RP.
  - DR election mechanism: DRs are responsible for receiving and sending multicast messages from the source or group members to prevent duplicate multicast packets. In addition, the receiver DR is responsible for sending Join messages.

Diagram description: The diagram shows the RPT being built from leaf routers up to the RP via Join messages (labeled "the DR election mechanism is used to determine the path" and "the RPT from leaf routers to the RP"), while multicast traffic flows separately from the source toward the RP once registration completes.

- The DR election mechanism in PIM-SM is similar to that in PIM-DM, and is not repeated here.

### RPT Establishment

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- An RPT is an MDT with the RP as the root, and PIM routers with group members as leaves.
- When a new group member appears on the network (an IGMP entry is generated), the receiver DR sends a Join message to the RP. The (*, G) entry is created hop by hop, and then an RPT with the RP as the root is generated.

Diagram description: A new group member appears behind the receiver DR. The receiver DR sends a Join message upstream toward the RP based on the unicast route to the RP. Each router along the path creates a (*, G) entry with the RP as root, generating multicast routing table entries hop by hop, until the RPT is fully established from the RP down to the receiver DR.

### Multicast Source Registration Mechanism — SPT Generation

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- In the PIM-SM (ASM) model, the MDT from the source DR to the RP cannot be established through Join messages. The multicast source registration mechanism is required to help establish the SPT from the source DR to the RP.
- SPT establishment depends on Register and Join messages. The detailed process is as follows:
  - The source DR encapsulates multicast data into unicast Register messages and sends them to the RP address.
  - Upon receiving the Register message, the RP decapsulates it, obtains the source address, and sends a Join message hop by hop back toward the source DR based on the source IP address, establishing an SPT reversely from the RP to the source DR.
  - Once the SPT is established, subsequent multicast data can travel natively along the SPT from the source DR to the RP.

Diagram description: The source DR encapsulates multicast data from the source into a Register message (shown as a unicast packet) and forwards it hop by hop to the RP. In parallel/response, the RP sends a Join message hop by hop back down toward the source DR, establishing the reverse SPT; the diagram marks the Register message path with one arrow style and the Join message / resulting SPT with another.

- IGMP does not run between the multicast source and the source DR. As a result, no PIM (*, G) entries can be generated through IGMP, and Join messages cannot be sent to establish an MDT.

### Multicast Source Registration Mechanism — Multicast Data Forwarding

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- After multicast source information is registered with the RP, an SPT from the multicast source to the RP is established. However, the source DR still encapsulates multicast data packets into Register messages, which may cause the following problems (double transmission of the same data, once natively along the SPT and once encapsulated in Register messages).
- After the SPT is established, the RP sends Register-Stop messages to instruct the source DR to send subsequent packets in native multicast mode instead of encapsulating them in Register messages.

Diagram description: The RP, having received duplicate multicast data both natively (via the newly built SPT) and encapsulated in Register messages, sends a Register-Stop message back to the source DR. After receiving it, the source DR stops encapsulating and sends multicast packets directly (natively) along the SPT toward the RP.

### Duplicate Multicast Packets on the Source End or Receiver End Network

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- On the source end or receiver end network, multiple multicast routers may forward multicast traffic, causing duplicate multicast packets.
- Only the PIM DR forwards multicast packets on the source end or receiver end network, which prevents duplicate multicast packets.

Diagram description: Two routers (DR and non-DR) sit on the same multi-access segment on either the source end or receiver end network. Both could potentially forward the same multicast packets onto the shared segment, but only the elected DR's interface is marked active for forwarding; the non-DR's interface is marked inactive, preventing duplicate packets from reaching the segment.

### PIM DR Election

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- PIM DR election:
  - In PIM-SM (ASM), routers compare the priorities and IP addresses carried in Hello messages to elect a DR for a multi-access network.
  - The router with the highest DR priority becomes the DR on the multi-access network. If multiple routers have the same DR priority, the router with the highest interface IP address becomes the DR.
  - If the DR fails, a new DR will be elected among the other routers.

Diagram description: Multiple routers on a shared multicast network exchange PIM Hello messages carrying DR priority and interface IP address. The DR is elected based on these values (highest priority, tie-broken by highest IP address); only the elected DR forwards multicast traffic onto the network, while the non-DR routers do not forward.

- The default DR priority is 1. A larger value indicates a higher priority.
- If multiple multicast routers are deployed on the receiver end network, both IGMP and PIM must be enabled on the downstream interfaces of the multicast routers.
- The DR can also act as an IGMPv1 querier.

### RPT Being a Sub-optimal Path

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- On a PIM-SM network, a multicast group corresponds to only one RP. Therefore, multicast data is sent to and forwarded by the RP, causing the following problems:
  - A large volume of multicast traffic causes a heavy burden on the RP.
  - The multicast forwarding path may be a sub-optimal one.

Diagram description: A topology shows the multicast source's traffic taking a longer path through the RP (via RT2 → RT4 → RT5, as labeled) to reach the group member, when a more direct path between source and receiver exists but is not used because all traffic must transit the RP — illustrating the sub-optimal RPT path problem.

### RPT-to-SPT Switchover Mechanism

*(Running header: MDT Establishment | MDT Optimization | MDT Maintenance)*

- After receiving multicast data, the RP sends the data to the receiver DR along the RPT. To prevent the potentially sub-optimal path (RPT) from being used to forward multicast packets, the receiver DR establishes an SPT from itself to the source based on the source IP address in the multicast data packets.
- The detailed process is as follows: the receiver DR learns the source's IP address from the multicast data it receives via the RPT, then sends a Join message hop by hop directly toward the source (rather than toward the RP), building a new, shorter SPT. Once the SPT is established, the receiver DR sends a Prune message toward the RP to prune itself off the RPT, so that traffic thereafter flows along the shortest path (SPT) instead of via the RP.

Diagram description: The receiver DR is shown building a direct SPT toward the true source (bypassing the RP) based on the source address seen in received multicast data, sending Join messages hop-by-hop along the shortest path. Once established, a Prune message is sent along the old RPT path toward the RP, removing the now-redundant RPT branch.

- A device sends Join messages along the shortest path through the upstream interface which is determined based on RPF election rules.
- On a multi-access network, duplicate packets may exist during the RPT-to-SPT switchover. The assert mechanism needs to be used to quickly select a downstream interface.
- Conditions for triggering the RPT-to-SPT switchover: When the forwarding rate exceeds the switchover threshold, the receiver's DR sends a Join message to the source, triggering the SPT switchover.

### MDT Maintenance (PIM-SM ASM)

- When an MDT (SPT or RPT) becomes stable, the receiver DR periodically sends Join/Prune messages to maintain the MDT.
- If there is no multicast traffic for a period of time (210s by default), the SPT disappears, and the receiver DR restores the RPT. Prune messages are sent at an interval of 60s to maintain the interface forwarding status.

## Implementation of PIM-SM (SSM)

### Contents (section marker)

1. Introduction to PIM
2. Introduction to PIM-DM
3. Introduction to PIM-SM
   - Implementation of PIM-SM (ASM)
   - **Implementation of PIM-SM (SSM)**
   - Basic PIM-SM Configurations

### SSM Concept Review

- The SSM model provides services for the data flows from a specific source to a specific group. When a receiver host joins a multicast group, the receiver host can specify the sources from which it wants to receive multicast data, or the sources from which it rejects multicast data. After joining a group, the hosts receive only the data sent from the specified sources to the group.
- The SSM model does not require globally unique group addresses, but the multicast source must be unique. Different applications on a source must use different SSM group addresses. Different applications on different sources can reuse SSM group addresses because each source-group pair has an (S, G) entry. This model saves multicast group addresses without congesting the network.

Diagram description: A multicast source and a multicast group member are shown, with the group member specifying exactly which source it wants to receive multicast data from (an (S, G) pairing), illustrating that the SSM model ties the group address to a specific known source rather than any source.

### PIM-SM (SSM) Overview

- Because the SSM model predefines a multicast source address, PIM-SM (SSM) allows the receiver DR to establish a reverse SPT based on the multicast source address.
- PIM-SM (SSM) does not require an RP, RPT, or multicast source registration. Instead, PIM-SM (SSM) can directly establish an SPT between a multicast source and group members.
- The key mechanisms of PIM-SM (SSM) include neighbor discovery, DR election, and SPT establishment.

Diagram description: The multicast group member, already knowing the specific source address for the group it wants, has its receiver DR send a Join message directly hop-by-hop toward the source DR (not toward any RP), establishing an SPT in reverse straight from the receiver to the source.

- PIM-SM (SSM) does not require the Assert mechanism.

### MDT Generation and Maintenance (PIM-SM SSM)

- The PIM-SM (SSM) model uses IGMPv3 messages and Join messages to establish an MDT.
- The MDT established in the PIM-SM (SSM) model always exists and will not disappear even if no multicast traffic is transmitted.

Diagram description: A group member sends an IGMPv3 Report message (specifying the desired source) to its local receiver DR. The receiver DR creates a (S, G) entry in its multicast routing table and sends a Join message hop-by-hop toward the source, establishing and permanently maintaining the SPT regardless of whether multicast traffic is currently flowing.

- The DR and neighbor discovery mechanisms in PIM-SM (SSM) are the same as those in PIM-DM, and are not repeated here.

### PIM Model Comparison

| Protocol | Model | Usage Scenario | Implementation |
|---|---|---|---|
| PIM-DM | ASM model | Small-scale LANs where multicast group members are distributed densely | Using the flooding-prune mechanism, PIM-DM creates and maintains a unidirectional loop-free SPT connecting a multicast source and group members. |
| PIM-SM | ASM model | Large-scale networks where multicast group members are distributed sparsely | An MDT is established through proactive notification of receivers' group-joining information. PIM-SM requires RP maintenance, RPT establishment, and multicast source registration. |
| PIM-SM | SSM model | Scenarios where user hosts know the exact locations of multicast sources in advance and can specify the sources from which they want to receive data before they join multicast groups | In PIM-SSM, an SPT is directly established between the multicast source and group members. PIM-SSM does not require RP maintenance, RPT establishment, or multicast source registration. |

## Basic PIM-SM Configurations

### Contents (section marker)

1. Introduction to PIM
2. Introduction to PIM-DM
3. Introduction to PIM-SM
   - Implementation of PIM-SM (ASM)
   - Implementation of PIM-SM (SSM)
   - **Basic PIM-SM Configurations**

### Basic PIM-SM Configurations

1. Enable multicast routing on a router.
```
[Huawei] multicast routing-enable
```
2. Enable PIM-SM on an interface.
```
[Huawei-GigabitEthernet1/0/0] pim sm
```
3. Configure a static RP.
```
[Huawei-pim] static-rp rp-address
```

### PIM-SM Basics Experiment

- Experiment requirements:
  - Configure PIM-SM to allow PC1 and PC2 to receive data packets from the multicast source.

Diagram description: A topology with a multicast source (10.10.10.10/24) connected via Loopback0 (1.1.1.1/32) and GE0/0/0 to router R1. R1 connects to R2 via GE0/0/0 on each side, and to R3 via GE0/0/1 (R1) to GE0/0/0 (R3). R2's GE0/0/1 connects to PC1 (Receiver, joined to multicast group 224.1.1.1). R3's GE0/0/1 connects to PC2 (Receiver, also joined to multicast group 224.1.1.1). (This is the same physical topology as the PIM-DM experiment, with a Loopback0 interface at 1.1.1.1/32 added, which is used as the static RP address.)

### PIM-SM Configurations (1)

Configurations on R1:
```
[R1] multicast routing-enable
[R1] interface g0/0/2
[R1-GigabitEthernet0/0/2] pim sm
[R1-GigabitEthernet0/0/2] interface g0/0/0
[R1-GigabitEthernet0/0/0] pim sm
[R1-GigabitEthernet0/0/0] interface g0/0/1
[R1-GigabitEthernet0/0/1] pim sm
[R1] pim
[R1-pim] static-rp 1.1.1.1
```

*(Configurations of interface IP addresses and OSPF are not provided here — as stated in the source slide.)*

### PIM-SM Configurations (2)

Configurations on R2:
```
[R2] multicast routing-enable
[R2] interface g0/0/0
[R2-GigabitEthernet0/0/0] pim sm
[R2-GigabitEthernet0/0/0] interface g0/0/1
[R2-GigabitEthernet0/0/1] igmp enable
[R2] pim
[R2-pim] static-rp 1.1.1.1
```

Configurations on R3:
```
[R3] multicast routing-enable
[R3] interface g0/0/0
[R3-GigabitEthernet0/0/0] pim sm
[R3-GigabitEthernet0/0/0] interface g0/0/1
[R3-GigabitEthernet0/0/1] igmp enable
[R3] pim
[R3-pim] static-rp 1.1.1.1
```

*(Configurations of interface IP addresses and OSPF are not provided here — as stated in the source slide.)*

## Quiz

1. (Single) What is the destination IP address of PIM messages? ( )
   - A. 224.0.0.2
   - B. 224.0.0.1
   - C. 224.0.0.5
   - D. 224.0.0.13
2. (Multiple) Which of the following mechanisms are used by multicast to prevent duplicate packets? ( )
   - A. RPF mechanism
   - B. Assert election mechanism
   - C. DR election mechanism
   - D. IGMP querier election mechanism

**Answers:** 1. D    2. ABC

## Summary

- There are two PIM models:
  - PIM-DM is mainly used on small-scale multicast networks with densely distributed users.
  - PIM-SM is mainly used on large-scale multicast networks with sparsely distributed users. Based on the multicast model, PIM-SM is classified into PIM-SM (ASM) and PIM-SM (SSM). PIM-SM (SSM) mainly serves SSM.
- PIM-DM uses the flooding-prune mechanism to establish an MDT. During MDT generation, PIM-DM uses the Assert and DR election mechanisms to prevent loops. During multicast forwarding, PIM-DM uses the RPF mechanism to prevent loops.
- PIM-SM (ASM) sends group membership information to the RP to establish an RPT. The multicast source sends multicast packets to the RP, which then sends multicast data to group members. In this manner, an MDT (SPT + RPT) is established. To prevent the RPT, which is a sub-optimal path, from being used to forward multicast traffic, PIM-SM (ASM) initiates an RPT-to-SPT switchover to optimize the MDT.
- PIM-SM (SSM) mainly serves the SSM model. Because this model knows the multicast source address in advance, an MDT can be directly established reversely.

*(Note on this slide's title: the source page did not display a repeated slide-box header before this content — consistent with the deck's pattern of a title slide followed by unheaded summary bullets. "Summary" is inferred from position (immediately after the Quiz and immediately before "Thank you") and from the standard Huawei course template structure; the bullet content itself is transcribed verbatim from the slide.)*

---

*(This file ends here, at document page 885 — the "Thank you" slide on document page 886 is a standard closing slide with no course-specific content, and is omitted per the H2/H3 structural rule of this transcription; let me know if you'd like it included as a final line.)*

